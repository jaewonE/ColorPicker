# ColorPicker 1.0.0

ColorPicker is a native macOS utility that reads the color at the current mouse position.

- Six output formats: RGB, normalized RGB, sRGB, normalized sRGB, Display P3, and Hex
- Apertures from 1 × 1 through 32 × 32 physical pixels, using a linear-light representative average
- Pixelated magnifier with 0.5×–8× area zoom
- Click-to-copy values
- Global Command-Shift-F coordinate lock and Command-Shift-S copy shortcuts

The ZIP is ad-hoc signed for Apple Silicon macOS 14+. On first use, grant Screen Recording permission so macOS can provide on-screen pixel data.

---

# ColorPicker 1.0.0 (한국어)

ColorPicker는 현재 마우스 위치의 색상을 읽는 macOS 네이티브 유틸리티입니다.

- RGB, 정규화 RGB, sRGB, 정규화 sRGB, Display P3, Hex의 6개 형식
- 선형 광량 평균을 사용하는 1 × 1부터 32 × 32 실제 픽셀까지의 조리개
- 0.5×–8× 영역 확대를 갖춘 픽셀 미리보기
- 값 영역 클릭으로 클립보드 복사
- 전역 Command-Shift-F 좌표 고정, Command-Shift-S 복사 단축키

ZIP은 Apple Silicon용 macOS 14 이상에서 동작하도록 ad-hoc 서명되어 있습니다. 최초 사용 시 화면 픽셀 데이터를 읽을 수 있도록 화면 기록 권한을 허용해야 합니다.
